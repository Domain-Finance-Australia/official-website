---
import { Image } from "astro:assets";

interface Props {
  src?: string | null;
  alt: string | null;
  width: number;
  height: number;
  class?: string;
  fill?: boolean;
}
const { src, alt, width, height, class: className, fill } = Astro.props;

const imageSrc = src ? src : "/images/fallback.png";
---

<Image
  src={imageSrc}
  alt={alt ?? "Image"}
  width={width}
  height={height}
  class={className}
  style={!fill && `width: ${width}px; height: ${height}px;`}
/>
